import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'Trax.Flagship.Inspect',
  appName: 'rent-acar-front-end',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
